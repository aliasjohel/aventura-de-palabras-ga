const {chromium}=require('C:/Users/User/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'});const p=await b.newPage({viewport:{width:Number(process.env.QA_WIDTH)||844,height:Number(process.env.QA_HEIGHT)||390},serviceWorkers:'block'});await p.goto('http://127.0.0.1:8789',{waitUntil:'load'});await p.evaluate(()=>{document.querySelector('#introOficial')?.remove();document.body.classList.remove('intro-pendiente');mostrarPantalla(pantallaVersus);void reproducirSiglosEnUnSegundoVersus('mago');clearTimeout(demoVersus.temporizadorCinematica);});await p.waitForFunction(()=>[...document.querySelectorAll('.kairos-final-victima,.kairos-final-invocador')].every(i=>i.complete));const sizes=await p.evaluate(()=>[...document.querySelectorAll('.kairos-final-victima,.kairos-final-invocador')].map(i=>{let c=document.createElement('canvas');c.width=i.naturalWidth;c.height=i.naturalHeight;let x=c.getContext('2d');x.drawImage(i,0,0);let d=x.getImageData(0,0,c.width,c.height).data,min=c.height,max=0;for(let y=0;y<c.height;y++)for(let z=0;z<c.width;z++)if(d[(y*c.width+z)*4+3]>100){min=Math.min(min,y);max=Math.max(max,y);}let s=getComputedStyle(i),ratio=Math.min(parseFloat(s.width)/c.width,parseFloat(s.height)/c.height);return {id:i.id,natural:[c.width,c.height],css:[s.width,s.height],alpha:[min,max],visualHeight:(max-min)*ratio*(s.scale==='none'?1:Number(s.scale))};}));const heights=sizes.map(s=>s.visualHeight);if(Math.max(...heights)/Math.min(...heights)>1.03)throw Error('Visible character heights differ');console.log('PASS visible heights within 3%',heights);await p.evaluate(()=>document.getAnimations().forEach(a=>{a.pause();a.currentTime=3500;}));await p.screenshot({path:'tools/.kairos-new-size.png'});await b.close();})().catch(e=>{console.error(e);process.exitCode=1});
