const fs=require('fs');const rate=44100,duration=1.12,n=Math.floor(rate*duration),b=Buffer.alloc(44+n*2);b.write('RIFF');b.writeUInt32LE(36+n*2,4);b.write('WAVEfmt ',8);b.writeUInt32LE(16,16);b.writeUInt16LE(1,20);b.writeUInt16LE(1,22);b.writeUInt32LE(rate,24);b.writeUInt32LE(rate*2,28);b.writeUInt16LE(2,32);b.writeUInt16LE(16,34);b.write('data',36);b.writeUInt32LE(n*2,40);let seed=739,low=0,mid=0;for(let i=0;i<n;i++){const t=i/rate;seed=(Math.imul(seed,1664525)+1013904223)>>>0;const noise=seed/4294967296*2-1;low+=.023*(noise-low);mid+=.2*(noise-mid);const crack=noise*Math.exp(-t*48)*.6;const body=mid*Math.exp(-t*5.5)*.8;const rumble=low*Math.exp(-t*2.8)*2.8*(.8+.2*Math.sin(t*27));const sub=Math.sin(2*Math.PI*(49*t+1.7*(1-Math.exp(-t*8))))*Math.exp(-t*5)*.15;const env=Math.min(1,t/.0015)*Math.min(1,(duration-t)/.15);const value=Math.tanh((crack+body+rumble+sub)*1.4)*env;b.writeInt16LE(Math.round(value*28000),44+i*2);}fs.writeFileSync('assets/sounds/trueno-cinematica-v2.wav',b);
